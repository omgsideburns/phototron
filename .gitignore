# Keep intentionally empty directories
!.gitkeep

# Ignore Venv
venv

# Keep personal info safe
account.py

# Keep default generated folders for some reason..
!events/default/
!events/default/raw/
!events/default/comps/

##################################
### Ignore all generated files ###
##################################

# macOS system files (macos loves to leave messes...)
.DS_Store
*__pycache__*
user_config.cfg
# don't sync testing contents
events/test*
events/default/raw/*
events/default/comps/*
